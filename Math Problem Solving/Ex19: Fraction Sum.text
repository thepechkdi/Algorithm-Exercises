BEGIN
    WRITE "Enter n:"
    READ n

    S = 0

    FOR i = 1 TO n DO
        IF i MOD 2 = 1 THEN          // odd
            S = S + (1 / (i * i))
        ELSE                         // even
            S = S + (1 / i)
        END IF
    END FOR

    WRITE "Sum S =", S
END
